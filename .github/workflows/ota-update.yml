name: Publish Fleet OTA Update

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'app/**'
      - 'src/**'
      - 'scripts/**'
      - 'app.config.ts'
      - 'eas.json'
      - '.github/workflows/ota-update.yml'

permissions:
  contents: read

concurrency:
  group: fleet-production-ota
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      EAS_PROJECT_ID: 90d1d6ff-1376-4065-a00c-7cf0415e4347
      EXPO_PUBLIC_OTA_CHANNEL: fleet-production
      EXPO_PUBLIC_SUPABASE_URL: https://ssgesjzdvdsqacdtasje.supabase.co
      EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY: sb_publishable_f1rbczgvWKSQy2g9mTDQZg_K9Wv19bL
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Verify Fleet OTA credential
        run: test -n "$EXPO_TOKEN" || (echo "EXPO_TOKEN is not configured in Kleenest_Fleet" && exit 1)
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Verify Expo authentication
        run: npx eas-cli@latest whoami --non-interactive
      - name: Verify dedicated Fleet Expo identity
        run: |
          npx expo config --type public --json > expo-config.json
          node <<'NODE'
          const fs=require('fs');
          const c=JSON.parse(fs.readFileSync('expo-config.json','utf8'));
          const project='90d1d6ff-1376-4065-a00c-7cf0415e4347';
          const channel='fleet-production';
          const failures=[];
          if(c.slug!=='kleenest-fleet')failures.push(`slug=${c.slug}`);
          if(c.extra?.eas?.projectId!==project)failures.push(`projectId=${c.extra?.eas?.projectId}`);
          if(c.updates?.url!==`https://u.expo.dev/${project}`)failures.push(`updates.url=${c.updates?.url}`);
          if(c.updates?.requestHeaders?.['expo-channel-name']!==channel)failures.push(`channel=${c.updates?.requestHeaders?.['expo-channel-name']}`);
          if(c.runtimeVersion!=='kleenest-fleet-0.1.0')failures.push(`runtimeVersion=${c.runtimeVersion}`);
          if(failures.length)throw new Error(`Fleet Expo identity mismatch: ${failures.join(', ')}`);
          console.log('Fleet Expo identity and OTA channel verified.');
          NODE
      - name: Verify Fleet release source
        run: |
          npm run audit
          npm run typecheck
      - name: Publish Fleet OTA update
        run: npx eas-cli@latest update --channel fleet-production --message "Fleet ${GITHUB_SHA}" --non-interactive
      - name: Verify Fleet OTA is discoverable
        run: |
          npx eas-cli@latest update:list --branch fleet-production --limit 1 --json --non-interactive > ota-latest.json
          grep -q "$GITHUB_SHA" ota-latest.json
